"""Streamlit 로컬 대시보드 — 옵션 의존성.

설치: `uv sync --extra dashboard` 또는 `pip install streamlit altair`.
실행: `uv run stock-compass dashboard` 또는
      `streamlit run src/stock_compass/dashboard/app.py`.
"""
